---
description: AutoGemini Workflow
---
This workflow is used to automatically generate content and site updates using the AutoGemini system.
1. Run `mix run import_all_tweets.exs`
2. Run `npx gh-pages -d output`
