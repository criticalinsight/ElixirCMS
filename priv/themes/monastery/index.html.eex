<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= @page_title %> - <%= @site.title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:opsz,wght@9..40,400;500;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'DM Sans', sans-serif; background-color: #FAFAFA; color: #1a1a1a; }
        .card-shadow { box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05), 0 2px 4px -1px rgba(0, 0, 0, 0.03); }
        .card-hover:hover { transform: translateY(-4px); box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04); }
    </style>
</head>
<body class="min-h-screen flex flex-col">

    <!-- Navigation -->
    <nav class="bg-white border-b border-gray-100 py-6">
        <div class="max-w-7xl mx-auto px-6 flex items-center justify-between">
            <a href="<%= @site.url %>" class="flex items-center gap-3">
                <div class="w-8 h-8 bg-black rounded-lg"></div>
                <span class="text-xl font-bold tracking-tight"><%= @site.title %></span>
            </a>
            
            <div class="hidden md:flex items-center gap-8 text-sm font-medium text-gray-500">
                <a href="<%= @site.url %>" class="text-black">Kenniscentrum</a>
                <a href="#" class="hover:text-black transition-colors">Over ons</a>
                <a href="#" class="hover:text-black transition-colors">Contact</a>
                <a href="#" class="px-5 py-2.5 bg-black text-white rounded-full hover:bg-gray-800 transition-colors">Start Project</a>
            </div>
        </div>
    </nav>

    <main class="flex-grow">
        <!-- Header Section -->
        <header class="bg-white pb-20 pt-16 border-b border-gray-100">
            <div class="max-w-7xl mx-auto px-6">
                <div class="max-w-3xl">
                    <h5 class="text-sm font-bold uppercase tracking-widest text-gray-400 mb-6">Knowledge Center</h5>
                    <h1 class="text-5xl md:text-6xl font-bold leading-tight mb-8 text-[#1a1a1a]">
                        Insights & Knowledge<br>
                        <span class="text-gray-400">for digital growth.</span>
                    </h1>
                    <p class="text-xl text-gray-500 leading-relaxed mb-10 max-w-2xl">
                        Discover our latest articles on strategy, design, and technology. 
                        Curated by our team of experts.
                    </p>
                    
                    <!-- Search / Filter Simulation -->
                    <div class="flex flex-wrap gap-3">
                        <button class="px-5 py-2 rounded-full bg-black text-white text-sm font-medium">All Articles</button>
                        <button class="px-5 py-2 rounded-full bg-gray-100 text-gray-600 text-sm font-medium hover:bg-gray-200 transition-colors">Strategy</button>
                        <button class="px-5 py-2 rounded-full bg-gray-100 text-gray-600 text-sm font-medium hover:bg-gray-200 transition-colors">Design</button>
                        <button class="px-5 py-2 rounded-full bg-gray-100 text-gray-600 text-sm font-medium hover:bg-gray-200 transition-colors">Development</button>
                    </div>
                </div>
            </div>
        </header>

        <!-- Content Grid -->
        <section class="max-w-7xl mx-auto px-6 py-20">
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                <%= for post <- @posts do %>
                <article class="bg-white rounded-2xl p-8 card-shadow card-hover transition-all duration-300 flex flex-col h-full group border border-gray-100/50">
                    <a href="<%= @site.url %><%= post.slug %>/index.html" class="absolute inset-0 z-10"></a>
                    
                    <div class="mb-8">
                        <span class="inline-block px-3 py-1 rounded-full bg-gray-100 text-xs font-bold uppercase tracking-wide text-gray-600 group-hover:bg-black group-hover:text-white transition-colors">
                            Article
                        </span>
                    </div>

                    <h2 class="text-2xl font-bold mb-4 leading-tight group-hover:text-amber-600 transition-colors">
                        <%= post.title %>
                    </h2>
                    
                    <p class="text-gray-500 mb-8 line-clamp-3 leading-relaxed">
                        <%= post.excerpt || "Read more about this topic in our detailed analysis." %>
                    </p>

                    <div class="mt-auto flex items-center justify-between pt-8 border-t border-gray-100">
                        <div class="flex items-center gap-3">
                             <div class="w-8 h-8 rounded-full bg-gray-200"></div>
                             <div class="text-xs font-bold">Admin</div>
                        </div>
                        <span class="text-xs font-medium text-gray-400">
                            <%= Calendar.strftime(post.published_at, "%b %d, %Y") %>
                        </span>
                    </div>
                </article>
                <% end %>
            </div>
            
            <div class="mt-20 text-center">
                <button class="px-8 py-3 border-2 border-gray-200 rounded-full text-sm font-bold hover:border-black transition-colors">Load More Articles</button>
            </div>
        </section>
    </main>

    <footer class="bg-[#1a1a1a] text-white py-20">
        <div class="max-w-7xl mx-auto px-6 grid grid-cols-1 md:grid-cols-4 gap-12">
            <div class="col-span-1 md:col-span-2">
                <h3 class="text-2xl font-bold mb-6"><%= @site.title %></h3>
                <p class="text-gray-400 max-w-sm">
                    We create digital experiences that matter. Strategy, Design & Development under one roof.
                </p>
            </div>
            <div>
                <h4 class="font-bold mb-6">Services</h4>
                <ul class="space-y-4 text-gray-400 text-sm">
                    <li><a href="#" class="hover:text-white">Digital Strategy</a></li>
                    <li><a href="#" class="hover:text-white">UX/UI Design</a></li>
                    <li><a href="#" class="hover:text-white">Development</a></li>
                </ul>
            </div>
            <div>
                <h4 class="font-bold mb-6">Contact</h4>
                <ul class="space-y-4 text-gray-400 text-sm">
                    <li>hello@example.com</li>
                    <li>+31 (0) 20 123 4567</li>
                    <li>Amsterdam, NL</li>
                </ul>
            </div>
        </div>
    </footer>

</body>
</html>
