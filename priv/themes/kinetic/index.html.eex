<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= @page_title %> /// <%= @site.title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://api.fontshare.com/v2/css?f[]=clash-display@200,400,700,600&display=swap" rel="stylesheet">
    <style>
        :root {
            --accent: <%= @theme.config["accent_color"] %>;
        }
        body { font-family: 'Clash Display', sans-serif; background: #fff; color: #000; }
        .brutal-border { border: 3px solid #000; box-shadow: 6px 6px 0px #000; }
        .brutal-hover:hover { transform: translate(-2px, -2px); box-shadow: 8px 8px 0px #000; }
        .bg-accent { background-color: var(--accent); }
        .text-accent { color: var(--accent); }
    </style>
</head>
<body class="selection:bg-black selection:text-white">

    <header class="border-b-4 border-black p-6 flex justify-between items-center sticky top-0 bg-white z-50">
        <a href="<%= @site.url %>" class="text-4xl font-bold uppercase tracking-tighter hover:text-accent transition-colors">
            <%= @site.title %>
        </a>
        <div class="hidden md:block text-xl font-bold">
            EST. <%= Date.utc_today().year %>
        </div>
    </header>

    <div class="border-b-4 border-black overflow-hidden whitespace-nowrap bg-accent py-3">
        <div class="inline-block animate-[marquee_20s_linear_infinite] text-2xl font-bold uppercase">
            BREAKING NEWS /// LATEST UPDATES /// READ NOW /// <%= @site.title %> /// 
            BREAKING NEWS /// LATEST UPDATES /// READ NOW /// <%= @site.title %> ///
        </div>
    </div>

    <main class="grid grid-cols-1 md:grid-cols-12 min-h-screen">
        <!-- Sidebar / Intro -->
        <div class="md:col-span-4 lg:col-span-3 border-r-4 border-black p-8 bg-yellow-300">
            <h1 class="text-6xl font-bold leading-[0.85] mb-8 uppercase">
                Raw.<br>Unfiltered.<br>Content.
            </h1>
            <p class="text-xl font-medium border-t-4 border-black pt-6">
                A brutalist approach to digital publishing. No fluff, just heavy typography and bold ideas.
            </p>
        </div>

        <!-- Content Grid -->
        <div class="md:col-span-8 lg:col-span-9 bg-white">
            <div class="grid grid-cols-1 md:grid-cols-2">
                <%= for {post, idx} <- Enum.with_index(@posts) do %>
                <article class="border-b-4 border-black <%= if rem(idx, 2) == 0, do: "md:border-r-4", else: "" %> p-8 group hover:bg-black hover:text-white transition-colors cursor-pointer">
                    <div class="flex justify-between items-start mb-12">
                        <span class="font-bold text-xl">0<%= idx + 1 %></span>
                        <span class="font-mono text-sm"><%= Calendar.strftime(post.published_at, "%d/%m/%Y") %></span>
                    </div>
                    <h2 class="text-4xl font-bold uppercase leading-none mb-6">
                        <a href="<%= @site.url %><%= post.slug %>/index.html" class="before:absolute before:inset-0">
                            <%= post.title %>
                        </a>
                    </h2>
                    <p class="text-lg font-medium opacity-100 group-hover:opacity-80">
                        <%= post.excerpt || "Click to read full story." %>
                    </p>
                    <div class="mt-8 text-right">
                        <span class="inline-block px-4 py-2 border-2 border-black group-hover:border-white font-bold uppercase text-sm">Read More -></span>
                    </div>
                </article>
                <% end %>
            </div>
        </div>
    </main>

    <footer class="bg-black text-white p-12 text-center text-4xl font-bold uppercase">
        <%= @site.title %> &copy; ALL RIGHTS RESERVED.
    </footer>

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    keyframes: {
                        marquee: {
                            '0%': { transform: 'translateX(0%)' },
                            '100%': { transform: 'translateX(-100%)' },
                        }
                    }
                }
            }
        }
    </script>
</body>
</html>
