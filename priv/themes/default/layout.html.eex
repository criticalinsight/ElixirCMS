<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= @page_title %> | <%= @site["title"] || "Haiku Garden" %></title>

    <%= if assigns[:post] do %>
      <meta name="description" content="<%= @post["excerpt"] || String.slice(@post["content_md"] || "", 0, 160) %>" />
      <meta property="og:title" content="<%= @post["title"] %>" />
      <meta property="og:type" content="article" />
    <% else %>
      <meta name="description" content="A collection of timeless Japanese poetry" />
    <% end %>

    <!-- Fonts: Merriweather (Serif) -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Merriweather:ital,wght@0,300;0,400;0,700;0,900;1,300;1,400;1,700;1,900&display=swap" rel="stylesheet">

    <!-- Pagefind Search -->
    <link href="<%= @relative_path %>_pagefind/pagefind-ui.css" rel="stylesheet">
    <script src="<%= @relative_path %>_pagefind/pagefind-ui.js"></script>

    <style>
      /* ===== SUHKA LITE DESIGN SYSTEM ===== */
      :root {
        /* Colors */
        --epcl-main-color: #2E4C3B; /* Deep Earthy Green */
        --epcl-secondary-color: #FDFBF9;
        --epcl-background-color: #FAF2EA; /* Cream */
        --epcl-titles-color: #202020;
        --epcl-text-color: #3D3D3D;
        --epcl-border-color: rgba(0,0,0,0.08);
        --epcl-accent-yellow: #FFC225;

        /* Typography */
        --epcl-font-family: 'Merriweather', serif;
        --epcl-title-font-family: 'Merriweather', serif;
        --epcl-font-size: 17px;
        --epcl-line-height: 1.8;

        /* Layout */
        --grid-container: 1190px;
        --section-padding: 60px;

        /* Shadows */
        --epcl-boxes-shadow: 0px 5px 20px 0px rgba(0,0,0, 0.05);
        --epcl-hover-shadow: 0px 10px 30px -5px rgba(0,0,0, 0.1);
      }

      * { box-sizing: border-box; margin: 0; padding: 0; }

      body {
        font-family: var(--epcl-font-family);
        font-size: var(--epcl-font-size);
        line-height: var(--epcl-line-height);
        color: var(--epcl-text-color);
        background: var(--epcl-background-color);
        -webkit-font-smoothing: antialiased;
      }

      .container {
        max-width: var(--grid-container);
        margin: 0 auto;
        padding: 0 24px;
      }

      /* ===== HEADER ===== */
      header {
        padding: 40px 0;
        background: transparent;
      }

      header .inner {
        display: flex;
        align-items: center;
        justify-content: space-between;
      }

      .logo {
        font-family: var(--epcl-title-font-family);
        font-weight: 900;
        font-size: 2rem;
        color: var(--epcl-titles-color);
        text-decoration: none;
        letter-spacing: -1px;
      }

      .logo span {
        color: var(--epcl-main-color);
      }

      nav ul {
        display: flex;
        gap: 30px;
        list-style: none;
      }

      nav a {
        font-family: var(--epcl-font-family);
        font-weight: 700;
        font-size: 1rem;
        color: var(--epcl-titles-color);
        text-decoration: none;
        position: relative;
      }

      nav a::after {
        content: '';
        position: absolute;
        bottom: -5px;
        left: 0;
        width: 0%;
        height: 2px;
        background: var(--epcl-main-color);
        transition: width 0.3s ease;
      }

      nav a:hover::after { width: 100%; }

      #search { min-width: 200px; }

      /* ===== MAIN ===== */
      main {
        padding: 40px 0 80px;
      }

      /* ===== GRID ===== */
      .grid-posts {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
        gap: 40px;
      }

      /* ===== CARDS ===== */
      .card {
        background: #fff;
        border-radius: 8px;
        overflow: hidden;
        box-shadow: var(--epcl-boxes-shadow);
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        display: flex;
        flex-direction: column;
      }

      .card:hover {
        transform: translateY(-5px);
        box-shadow: var(--epcl-hover-shadow);
      }

      .card-image-wrapper {
        position: relative;
        padding-top: 60%; /* Aspect matching common featured images */
        overflow: hidden;
      }

      .card-image {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 0.5s ease;
      }

      .card:hover .card-image { transform: scale(1.05); }

      .card-body {
        padding: 32px;
        flex: 1;
        display: flex;
        flex-direction: column;
      }

      .card-tags {
        margin-bottom: 12px;
      }

      .tag {
        font-size: 0.75rem;
        font-weight: 900;
        text-transform: uppercase;
        color: var(--epcl-main-color);
        text-decoration: none;
        letter-spacing: 1px;
        margin-right: 10px;
      }

      .card-title {
        font-family: var(--epcl-title-font-family);
        font-size: 1.5rem;
        font-weight: 700;
        margin-bottom: 16px;
        line-height: 1.3;
      }

      .card-title a {
        color: var(--epcl-titles-color);
        text-decoration: none;
        transition: color 0.2s;
      }

      .card-title a:hover { color: var(--epcl-main-color); }

      .card-excerpt {
        font-size: 1rem;
        color: #777;
        margin-bottom: 24px;
        flex: 1;
      }

      .card-meta {
        font-size: 0.85rem;
        color: #999;
        font-weight: 400;
        font-style: italic;
        display: flex;
        justify-content: space-between;
        align-items: center;
        border-top: 1px solid var(--epcl-border-color);
        padding-top: 20px;
        margin-top: auto;
      }

      /* ===== POST SINGLE ===== */
      .post-single {
        max-width: 780px;
        margin: 0 auto;
        background: #fff;
        padding: 60px;
        border-radius: 8px;
        box-shadow: var(--epcl-boxes-shadow);
      }

      .post-header {
        text-align: center;
        margin-bottom: 40px;
      }

      .post-title {
        font-size: 2.5rem;
        margin-top: 10px;
        margin-bottom: 20px;
      }

      .post-image-full {
        width: 100%;
        border-radius: 8px;
        margin-bottom: 40px;
      }

      .prose {
        font-size: 1.1rem;
      }

      .prose p { margin-bottom: 1.8em; }

      .prose h2 { font-size: 1.8rem; margin-top: 1.5em; margin-bottom: 0.8em; }

      /* ===== FOOTER ===== */
      footer {
        padding: 60px 0;
        text-align: center;
        font-size: 0.9rem;
        color: #888;
        border-top: 1px solid rgba(0,0,0,0.05);
      }

      footer a { color: var(--epcl-main-color); text-decoration: none; font-weight: 700; }
    </style>
  </head>
  <body>
    <header>
      <div class="container inner">
        <a href="<%= @relative_path %>index.html" class="logo">Suhka<span>Haiku</span></a>
        <nav>
          <ul>
            <li><a href="<%= @relative_path %>index.html">Home</a></li>
            <li><a href="<%= @relative_path %>feed.xml">RSS</a></li>
          </ul>
        </nav>
        <div id="search"></div>
      </div>
    </header>

    <main>
      <div class="container">
        <%= @inner_content %>
      </div>
    </main>

    <script>
      window.addEventListener('DOMContentLoaded', () => {
        if (typeof PagefindUI !== 'undefined') {
          new PagefindUI({ element: "#search", showSubResults: true, baseUrl: "<%= @relative_path %>" });
        }
      });
    </script>

    <footer>
      <div class="container">
        <p>&copy; <%= Date.utc_today().year %> <a href="<%= @relative_path %>index.html">Haiku Garden</a>. Designed with <span style="color: #e25555;">&hearts;</span> and Suhka Lite Style.</p>
      </div>
    </footer>
  </body>
</html>
